# Post Generation TODO:
 * tag the initial (repo, or package generation) commit with a `v0.0.0` tag, e.g.:
   ```bash
   git tag -a v0.0.0 -m "Marking zeroth release for auto-versioning and CI/CD Tooling"
   git push --tags
   ```
 * create a docker repository for your project in the `hyp3-full-access` account:
   ```bash
   # Assuming your aws cli is setup to use the hyp3-full-access profile
   aws ecr create-repository \
       --repository-name {{cookiecutter.project_name}} \
       --image-scanning-configuration scanOnPush=false \
       --region us-east-1
   ```
 *
